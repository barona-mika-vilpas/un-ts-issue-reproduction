# Reproduction for <https://github.com/un-ts/prettier/issues/292>

To reproduce this error, run

```sh
npm install
npm run prettier
```

You can also see the github actions workflow for this repo, which runs the same commands.
